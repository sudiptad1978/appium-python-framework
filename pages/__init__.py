"""Page Object Model.

Each screen of the application under test is one page class that exposes
business-level actions (``login(username, password)``) and hides locators
and wait logic. ``BasePage`` provides the shared toolbox.
"""
